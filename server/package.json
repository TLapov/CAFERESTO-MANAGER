{
  "name": "caferesto-manager-server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "build": "rm -rf ./build && tsc --build",
    "build:migrate": "rm -rf ./build-migration && tsc -p ./migration/tsconfig.migration.json",
    "dev": "nodemon server.ts",
    "migrate:dev": "nodemon ./migration/index.migration.ts",
    "start": "node ./build/server",
    "migrate": "node ./build-migration/migration/index.migration",
    "test": "nodemon ./test/index.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "mysql2": "^3.11.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.7.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.2"
  }
}
